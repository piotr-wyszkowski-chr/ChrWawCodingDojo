using System;

namespace PolidromeChecker
{
    public class PolidromeChecker
    {
        /*
Modify Check method to return a boolean (true/false) to indicate if the inputed string is a palindrome.

Example:

"ABBA" is a palindrome
"ABAB" is not a palindrome
         */
        public bool Check(string input)
        {
            
        }
    }
}